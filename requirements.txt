# Runtime dependencies
pandas>=2.0
openpyxl>=3.1
tkinterdnd2>=0.3   # drag-and-drop support (gracefully skipped if absent)

# Build dependency (only needed to produce the portable .app / .exe)
# pyinstaller>=6.0
